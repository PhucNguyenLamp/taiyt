services:
  backend:
    build:
      context: ./be
    environment:
      HOST: 0.0.0.0
      PORT: 3000
    ports:
      - "3000:3000"

  frontend:
    build:
      context: ./vite-project
    depends_on:
      - backend
    ports:
      - "8080:80"